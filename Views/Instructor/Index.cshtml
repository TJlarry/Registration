@model IEnumerable<CourseRegistration.Models.ModelsDto.InstructorDto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.I_instructorID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.I_firstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.I_lastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.I_emailAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.C_courseID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.I_instructorID)
            </td>
            <td><a href="#" data-bs-toggle="modal" data-bs-target="#exampleModal"  onclick="ShowInstructorInfo('@item.I_instructorID')">
                @Html.DisplayFor(modelItem => item.I_firstName)</a>
                
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.I_lastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.I_emailAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.C_courseID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Update", new { id=item.I_instructorID }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>


<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>
